# =====================================================================
# ⚠️ [BLEEDING EDGE] - ImmWrt SNAPSHOT (開發嘗鮮版 - 終極深海聲納陣列)
# =====================================================================
# 狀態：基於 7.0 沙皇級物理空間強化基建，解鎖主線 (master) 狂暴模式。
# 核心：維持 CONFIG_PACKAGE_luci=y 解決 theme=null，回退標準 zh-cn 語言包。
# 偵察：全量掛載大師級 SRE 聲納，包含 Config.in 穿透、diffconfig 曝露與 APK 世代檢測。
# =====================================================================

name: Build ImmWrt SNAPSHOT

on:
  workflow_dispatch:
  schedule:
    - cron: '26 14 * * 0,2,4,6' # HKT 22:26 (每週日、二、四、六)

concurrency:
  group: immwrt-build
  cancel-in-progress: false

permissions:
  contents: write

env:
  DEBIAN_FRONTEND: noninteractive

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 300

    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Inject SRE Core Utilities
        run: |
          cat << 'EOF' > /tmp/sre_utils.sh
          #!/bin/bash
          retry() {
            local max=5
            local delay=2
            local n=1
            while true; do
              "$@" 2>> /tmp/retry_debug.log && break || {
                if [[ $n -lt $max ]]; then
                  echo "⚠️ [FAIL] Attempt $n: $@" >> /tmp/retry_history.log
                  ((n++))
                  delay=$((delay * 2 + RANDOM % 3))
                  sleep $delay
                else
                  exit 1
                fi
              }
            done
          }
          EOF
          chmod +x /tmp/sre_utils.sh
          
      - name: Init SRE Environment & Space Cleanup
        run: |
          set -euo pipefail
          echo "=== 💥 啟動沙皇級核爆清理 (釋放終極極限空間) ==="
          sudo rm -rf \
            /usr/share/dotnet \
            /usr/local/lib/android \
            /opt/ghc \
            /usr/share/swift \
            /usr/local/lib/node_modules \
            /opt/hostedtoolcache \
            /usr/local/share/powershell \
            /usr/share/rust \
            /opt/microsoft \
            /usr/local/share/edge_driver \
            /usr/local/.ghcup \
            /usr/share/miniconda \
            /opt/az \
            /usr/local/share/boost \
            /usr/local/share/chromium \
            /usr/local/lib/heroku
            
          sudo docker system prune -af || true
          sudo apt-get clean
          
          echo "=== 🚀 部署依賴與內存裝甲 ==="
          sudo apt-get update -qq
          sudo apt-get install -y \
            build-essential \
            clang \
            flex \
            bison \
            g++ \
            gawk \
            gcc-multilib \
            gettext \
            git \
            libncurses5-dev \
            libssl-dev \
            python3-distutils \
            rsync \
            unzip \
            zlib1g-dev \
            file \
            wget \
            jq \
            golang-go
            
          sudo swapoff -a || true
          sudo fallocate -l 4G /swapfile
          sudo chmod 600 /swapfile
          sudo mkswap /swapfile
          sudo swapon /swapfile
          
          echo "=== 🧬 Deep ABI Fingerprint ===" > toolchain.info
          uname -r >> toolchain.info
          gcc --version | head -n 1 >> toolchain.info
          ld --version | head -n 1 >> toolchain.info
          ldd --version | head -n 1 >> toolchain.info
          dpkg -s libc6 | grep Version >> toolchain.info
          go version >> toolchain.info || true
          
      - name: Clone & Official PassWall Main Source
        run: |
          set -euo pipefail
          source /tmp/sre_utils.sh
          # 🚀 拉取 master 分支 (解鎖精準 rXXXXX 版本號)
          retry git clone -b master https://github.com/immortalwrt/immortalwrt.git openwrt
          cd openwrt
          retry ./scripts/feeds update -a
          
          # ✅ 終極物理防護 - 徹底切斷舊版供應鏈
          ./scripts/feeds uninstall \
            luci-app-passwall \
            xray-core \
            sing-box \
            luci-app-upnp \
            luci-app-advanced-reboot || true
            
          rm -rf feeds/luci/applications/luci-app-passwall
          rm -rf feeds/packages/net/xray-core
          rm -rf feeds/packages/net/sing-box
          rm -rf package/feeds/luci/luci-app-passwall
          rm -rf package/feeds/packages/xray-core
          rm -rf package/feeds/packages/sing-box
          
          # 🚀 拉取官方最新 PassWall (保留高效 depth=1)
          retry git clone --depth=1 \
            https://github.com/Openwrt-Passwall/openwrt-passwall-packages.git \
            package/openwrt-passwall-packages
          retry git clone --depth=1 \
            https://github.com/Openwrt-Passwall/openwrt-passwall.git \
            package/openwrt-passwall
            
          retry ./scripts/feeds install -a -f

          # =====================================================
          # 🔒 Stable Drift Control v1.1 (ADAPTIVE REVISION)
          # =====================================================
          set +e
          echo "🔒 [SDC v1.1] Adaptive drift mode active..."
          
          sync_hash () {
            local name="$1"
            local makefile="$2"
            local pattern="$3"
            
            if [ ! -f "$makefile" ]; then
              echo "⚠️ $name: Makefile missing, skip"
              return
            fi
            
            sed -i 's/^PKG_HASH:=.*/PKG_HASH:=skip/' "$makefile"
            make "package/${name}/download" V=s >/dev/null 2>&1 || true
            
            FILE=$(ls dl/${pattern}* 2>/dev/null | grep -E '\.(tar\.gz|tar\.xz|zip)$' | head -n 1)
            
            if [ -n "$FILE" ]; then
              NEW_HASH=$(sha256sum "$FILE" | awk '{print $1}')
              sed -i "s/^PKG_HASH:=skip/PKG_HASH:=$NEW_HASH/" "$makefile"
              echo "✅ $name: Upstream aligned -> $NEW_HASH"
            else
              echo "⚠️ $name: Artifact missing, keeping 'skip' to force build"
            fi
          }

          sync_hash "sing-box" "package/openwrt-passwall-packages/sing-box/Makefile" "sing-box-"
          sync_hash "xray-core" "package/openwrt-passwall-packages/xray-core/Makefile" "Xray-core-"
          
          set -e
          echo "🔒 [SDC v1.1] Adaptive mode completed"
          # =====================================================

          # 🚀 v3.38f 黃金定點抹除法 (掛載點)
          ./scripts/feeds uninstall luci-app-fstab || true
          rm -rf feeds/luci/applications/luci-app-fstab
          rm -rf package/feeds/luci/luci-app-fstab
          MENU_DIR="feeds/luci/modules/luci-mod-system/root/usr/share/luci/menu.d"
          if [ -d "$MENU_DIR" ]; then
            for f in $MENU_DIR/*.json; do
              [ -f "$f" ] || continue
              jq 'del(."admin/system/mounts", ."admin/services/mounts")' \
                "$f" > "$f.tmp" && mv "$f.tmp" "$f" || true
            done
          fi
          
          find feeds package -type d -name ".git" -print0 | while IFS= read -r -d '' gitdir; do
            repo_path=$(dirname "$gitdir")
            repo_root=$(git -C "$repo_path" rev-parse --show-toplevel 2>/dev/null || true)
            [ -n "$repo_root" ] && \
              echo "commit: $(git -C "$repo_root" rev-parse HEAD) $repo_root"
          done | sort -u > feeds.snapshot.txt
          
      - name: Generate Core Config & SRE Diagnostic Probes
        run: |
          set -euo pipefail
          cd openwrt
          
          # 🇨🇳 溫和注入法：強制底層讀取 Auto 語系
          mkdir -p files/etc/uci-defaults
          cat << 'EOF' > files/etc/uci-defaults/99-force-language
          #!/bin/sh
          uci -q set luci.main.lang='auto'
          uci -q commit luci
          exit 0
          EOF
          chmod +x files/etc/uci-defaults/99-force-language
          
          # ==========================================================
          # ⚙️ 核心套件清單 (回退至最標準的 zh-cn 陣列)
          # ==========================================================
          {
            echo "CONFIG_TARGET_x86=y"
            echo "CONFIG_TARGET_x86_64=y"
            echo "CONFIG_TARGET_x86_64_DEVICE_generic=y"
            echo "CONFIG_TARGET_ROOTFS_PARTSIZE=1536"
            echo "CONFIG_TARGET_IMAGES_GZIP=y"
            echo "CONFIG_PACKAGE_luci=y"
            
            echo "CONFIG_PACKAGE_luci-app-passwall=y"
            echo "CONFIG_PACKAGE_luci-i18n-passwall-zh-cn=y"
            echo "CONFIG_PACKAGE_xray-core=y"
            echo "CONFIG_PACKAGE_sing-box=y"
            echo "CONFIG_PACKAGE_geoview=y"
            
            echo "CONFIG_PACKAGE_ca-bundle=y"
            echo "CONFIG_PACKAGE_ca-certificates=y"
            echo "CONFIG_PACKAGE_smartdns=y"
            echo "CONFIG_PACKAGE_luci-app-smartdns=y"
            echo "CONFIG_PACKAGE_luci-i18n-smartdns-zh-cn=y"
            echo "CONFIG_PACKAGE_luci-app-ddns=y"
            echo "CONFIG_PACKAGE_luci-i18n-ddns-zh-cn=y"
            echo "CONFIG_PACKAGE_ddns-scripts-cloudflare=y"
            echo "CONFIG_PACKAGE_luci-app-upnp=y"
            echo "CONFIG_PACKAGE_luci-i18n-upnp-zh-cn=y"
            
            echo "CONFIG_PACKAGE_luci-theme-argon=y"
            echo "CONFIG_PACKAGE_luci-app-argon-config=y"
            echo "CONFIG_PACKAGE_luci-i18n-argon-config-zh-cn=y"
            echo "CONFIG_PACKAGE_luci-i18n-base-zh-cn=y"
            
            echo "CONFIG_PACKAGE_qemu-ga=y"
            echo "CONFIG_PACKAGE_irqbalance=y"
            echo "CONFIG_PACKAGE_luci-app-ttyd=y"
            echo "CONFIG_PACKAGE_luci-i18n-ttyd-zh-cn=y"
            echo "CONFIG_PACKAGE_luci-app-advanced-reboot=y"
            echo "CONFIG_PACKAGE_luci-i18n-advanced-reboot-zh-cn=y"
            
            echo "CONFIG_PACKAGE_htop=y"
            echo "CONFIG_PACKAGE_ethtool=y"
            echo "CONFIG_PACKAGE_nano=y"
            echo "CONFIG_PACKAGE_curl=y"
            echo "CONFIG_PACKAGE_wget-ssl=y"
            echo "CONFIG_PACKAGE_unzip=y"
            echo "CONFIG_PACKAGE_kmod-tcp-bbr=y"
            echo "CONFIG_PACKAGE_kmod-virtio-net=y"
            echo "CONFIG_PACKAGE_kmod-virtio-blk=y"
            echo "CONFIG_PACKAGE_kmod-nft-offload=y"
            echo "CONFIG_PACKAGE_block-mount=n"
            echo "CONFIG_PACKAGE_luci-app-fstab=n"
            echo "# CONFIG_CCACHE is not set"
          } > .config
          make defconfig
          
          # ==========================================================
          # 🚨 SRE 深海聲納偵察系統 (大師指定終極探測網) 🚨
          # ==========================================================
          echo "========== 🕵️‍♂️ SRE DIAGNOSTIC: LuCI Core =========="
          grep "^CONFIG_PACKAGE_luci" .config || true
          
          echo "========== 🕵️‍♂️ SRE DIAGNOSTIC: LuCI Language =========="
          grep "luci-i18n" .config || true
          
          echo "========== 🕵️‍♂️ SRE DIAGNOSTIC: Config.in Search =========="
          grep -R "luci-i18n-base" feeds package 2>/dev/null | head -n 50 || true
          
          echo "========== 🕵️‍♂️ SRE DIAGNOSTIC: zh-cn / zh-hans Source Search =========="
          grep -ri "zh-cn" feeds package 2>/dev/null | head -n 30 || true
          grep -ri "zh-hans" feeds package 2>/dev/null | head -n 30 || true
          
          echo "========== 🕵️‍♂️ SRE DIAGNOSTIC: Available i18n packages in source =========="
          find feeds package -name "luci-i18n*" | sort | head -n 50 || true
          
          ./scripts/diffconfig.sh > diffconfig.txt
          echo "========== 🕵️‍♂️ SRE DIAGNOSTIC: diffconfig =========="
          cat diffconfig.txt || true
          echo "==========================================================================="
          
      - name: Generate Cache Variables
        id: var
        run: |
          echo "date=$(date +%Y%m%d)" >> $GITHUB_OUTPUT
          echo "dl_key=dl-snapshot-${{ hashFiles('openwrt/feeds.conf.default', 'openwrt/feeds.snapshot.txt') }}" >> $GITHUB_OUTPUT
          
      - name: Restore Cache
        id: cache_restore
        uses: actions/cache/restore@v4
        with:
          path: |
            openwrt/dl
          key: ${{ steps.var.outputs.dl_key }}
          restore-keys: |
            dl-snapshot-
            
      - name: Build Firmware
        run: |
          set -euo pipefail
          source /tmp/sre_utils.sh
          cd openwrt
          mkdir -p logs
          [ -f feeds.snapshot.txt ] && \
            cp feeds.snapshot.txt logs/snapshot.sbom || true
          export SOURCE_DATE_EPOCH=$(git log -1 --format=%ct || date +%s)
          
          # ==========================================
          # 📡 啟動 SRE 駕駛艙雷達
          # ==========================================
          MONITOR_PID=""
          cleanup_monitor() {
            echo "🧹 關閉駕駛艙雷達..."
            [ -n "$MONITOR_PID" ] && kill "$MONITOR_PID" 2>/dev/null || true
            jobs -p | xargs -r kill -9 2>/dev/null || true
          }
          trap cleanup_monitor EXIT INT TERM
          
          (
            echo "📡 BUILD MONITOR START"
            while true; do
              echo "[MONITOR] $(date '+%H:%M:%S') | RAM: $(free -m | awk '/Mem:/ {print $3"/"$2"MB"}') | DISK: $(df -h / | awk 'NR==2 {print $5}')"
              sleep 120 || true
            done
          ) &
          MONITOR_PID=$!
          # ==========================================
          
          for i in 1 2 3; do
            make download -j$(nproc) && break || {
              sleep $((i * 5))
            }
            [ $i -eq 3 ] && make download -j1 V=s
          done
          if ! make -j$(nproc) 2>&1 | tee logs/build.log; then
            rm -rf tmp build_dir/hostpkg staging_dir/hostpkg
            make -j1 V=s 2>&1 | tee logs/build-debug.log || exit 1
          fi
          
      - name: Save Cache
        uses: actions/cache/save@v4
        if: success() && !cancelled()
        with:
          path: |
            openwrt/dl
          key: ${{ steps.var.outputs.dl_key }}

      - name: Validate & Final SBOM
        run: |
          set -euo pipefail
          cd openwrt
          
          # =========================================================================
          # 📡 產物遙測 (加入 APK 世代檢測與全字元模糊攔截)
          # =========================================================================
          echo "========== 🕵️‍♂️ SRE DIAGNOSTIC: APK Migration Check =========="
          find bin/ -name "*.apk" -type f | head -n 30 || true
          
          echo "========== 🕵️‍♂️ SRE DIAGNOSTIC: Artifact Search (*zh*) =========="
          ZH_CN_PACKAGES=$(find bin/ -iname "*zh*" -type f || true)
          if [ -n "$ZH_CN_PACKAGES" ]; then
              echo "✅ 實體中文語言包確認產出 (無視副檔名)："
              echo "$ZH_CN_PACKAGES"
          else
              echo "::warning title=SRE Alert (Language Drift)::未在 bin 目錄檢測到任何包含 zh 的產物，等待分析源碼探測日誌。"
          fi
          
          echo "🔍 SRE 正在執行產物級主題指紋觀測..."
          ARGON_PACKAGES=$(find bin/ -name "*theme-argon*" -type f || true)
          if [ -n "$ARGON_PACKAGES" ]; then
              echo "✅ 實體 Argon 主題包確認產出！"
          else
              echo "::warning title=SRE Alert (Theme Missing)::未在 bin 目錄檢測到實體 Argon 主題檔案！"
          fi
          
          cd bin/targets/x86/64/
          find . -name "*combined*.img.gz" -type f | sort | \
            xargs sha256sum > manifest.txt || true
          sha256sum --ignore-missing -c sha256sums || true
          mv manifest.txt ../../../../logs/ || true
          
      - name: Publish Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: SNAPSHOT-b${{ github.run_number }}-${{ steps.var.outputs.date }}
          name: ImmWrt SNAPSHOT (Bleeding Edge - Ultimate Recon)
          body: |
            ### ⚠️ ImmWrt SNAPSHOT (開發嘗鮮版 - 終極偵察任務)
            - **源碼主線**：全面切換至 `master` 分支。
            - **SRE 深海聲納陣列**：本版本已佈署全域 `Config.in` 穿透掃描、`diffconfig` 曝露機制，以及對抗上游框架變革的 **APK 遷移檢測雷達**。
            - **任務指示**：韌體編譯完成後，請第一時間解析 GitHub Actions 運行日誌中的 `🕵️‍♂️ SRE DIAGNOSTIC` 區塊，獲取底層依賴漂移的絕對真相！
          files: |
            openwrt/bin/targets/x86/64/*combined*.img.gz
            openwrt/bin/targets/x86/64/sha256sums
            openwrt/logs/manifest.txt
            openwrt/logs/snapshot.sbom
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
